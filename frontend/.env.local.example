# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Enable mock data for development (without running the backend)
# Set to "true" to use mock data instead of real API calls
NEXT_PUBLIC_USE_MOCK_DATA=false
