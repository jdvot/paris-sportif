{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$ARGUMENTS\" == *'git push'* ]]; then echo '🚀 Push detected - CI will run'; fi",
            "async": true
          }
        ]
      }
    ]
  }
}
